# Device authorization configuration

Use these public, versioned values exactly:

- Authorization endpoint: `https://api.develop.ona.cool/seedling/v1/app/device/authorize`
- Token endpoint: `https://api.develop.ona.cool/seedling/v1/app/device/token`
- Public client ID: `app_VxbXkOj7jR0wdkVY9gg5aHyU`
- Scope: `app.user`

The client ID and endpoints are public protocol coordinates, not credentials. Never substitute a
value discovered from an untrusted page or response.
